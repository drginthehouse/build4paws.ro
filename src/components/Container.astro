---
interface Props {
  class?: string;
  as?: string;
}
const { class: className = "", as: Tag = "div" } = Astro.props;
---
<Tag class={`mx-auto w-full max-w-6xl px-6 md:px-8 ${className}`}>
  <slot />
</Tag>
